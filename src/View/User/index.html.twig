{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li><a href="/user/show?id={{ user.id }}">{{ user.id }}. {{ user.nickname }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/user/add">Ajouter</a>
{% endblock %}